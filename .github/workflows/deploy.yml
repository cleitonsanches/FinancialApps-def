name: Deploy to VPS

on:
  push:
    branches:
      - main
    # Evitar deploy automático em cada push, apenas quando explicitamente necessário
    # Para ativar: descomente as linhas acima e comente o workflow_dispatch abaixo
  workflow_dispatch:
    # Permite deploy manual pelo GitHub Actions UI

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Validate secrets
        run: |
          if [ -z "${{ secrets.VPS_HOST }}" ]; then
            echo "❌ Erro: VPS_HOST secret não está configurado!"
            echo "Por favor, configure os secrets no GitHub:"
            echo "  Settings > Secrets and variables > Actions"
            echo "  Adicione: VPS_HOST, VPS_USER, VPS_SSH_KEY"
            exit 1
          fi
          if [ -z "${{ secrets.VPS_USER }}" ]; then
            echo "❌ Erro: VPS_USER secret não está configurado!"
            exit 1
          fi
          if [ -z "${{ secrets.VPS_SSH_KEY }}" ]; then
            echo "❌ Erro: VPS_SSH_KEY secret não está configurado!"
            exit 1
          fi
          echo "✅ Todos os secrets estão configurados"

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm install

      - name: Build API
        run: npm run build --workspace=apps/api

      - name: Build Web
        run: npm run build --workspace=apps/web

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || '22' }}
          timeout: 300s
          script: |
            # Ir para o diretório do projeto
            cd /var/www/FinancialApps-def
            
            # Fazer backup do banco de dados ANTES do deploy
            # Procurar banco em todos os locais possíveis
            DB_FOUND=""
            DB_BACKUP_DIR="backups"
            mkdir -p "$DB_BACKUP_DIR"
            TIMESTAMP=$(date +%Y%m%d_%H%M%S)
            
            # Verificar locais possíveis (ordem de prioridade)
            for DB_LOC in "apps/api/database.sqlite" "database.sqlite" "apps/api/../database.sqlite"; do
              if [ -f "$DB_LOC" ]; then
                DB_SIZE=$(du -h "$DB_LOC" | cut -f1)
                TABLES=$(sqlite3 "$DB_LOC" ".tables" 2>/dev/null | wc -w)
                echo "✅ Banco encontrado: $DB_LOC (Tamanho: $DB_SIZE, Tabelas: $TABLES)"
                
                # Fazer backup
                cp "$DB_LOC" "$DB_BACKUP_DIR/database.sqlite.backup.$TIMESTAMP"
                echo "✅ Backup criado: $DB_BACKUP_DIR/database.sqlite.backup.$TIMESTAMP"
                
                DB_FOUND="$DB_LOC"
                break
              fi
            done
            
            if [ -z "$DB_FOUND" ]; then
              echo "⚠️ AVISO: Nenhum banco de dados encontrado. Será criado um novo após o deploy."
            fi
            
            # Limpar arquivos não rastreados que podem causar conflito (como export/*.csv)
            echo "🧹 Limpando arquivos não rastreados na pasta export/..."
            if [ -d "export" ]; then
              rm -rf export/*.csv export/*.txt 2>/dev/null || true
              echo "✅ Arquivos CSV removidos da pasta export/"
            fi
            
            # Fazer pull das mudanças (usando estratégia para sobrescrever arquivos locais)
            echo "📥 Fazendo pull das mudanças..."
            git fetch origin main
            git reset --hard origin/main || {
              echo "⚠️ Erro ao fazer reset. Tentando merge..."
              git merge -X theirs origin/main || echo "⚠️ Erro no merge (continuando...)"
            }
            
            # Instalar dependências (se necessário)
            echo "📦 Instalando dependências..."
            npm install
            
            # Rebuild do projeto
            echo "🔨 Fazendo build da API..."
            npm run build --workspace=apps/api
            
            echo "🔨 Fazendo build do Web..."
            npm run build --workspace=apps/web
            
            # Verificar se o banco de dados existe APÓS o pull
            # NÃO criar banco novo se já existe um em qualquer local
            DB_EXISTS=false
            
            # Verificar locais possíveis
            for DB_LOC in "apps/api/database.sqlite" "database.sqlite"; do
              if [ -f "$DB_LOC" ]; then
                DB_SIZE=$(du -h "$DB_LOC" | cut -f1)
                TABLES=$(sqlite3 "$DB_LOC" ".tables" 2>/dev/null | wc -w)
                echo "✅ Banco existe: $DB_LOC (Tamanho: $DB_SIZE, Tabelas: $TABLES)"
                DB_EXISTS=true
                break
              fi
            done
            
            # SÓ criar banco se realmente não existir nenhum
            if [ "$DB_EXISTS" = false ]; then
              echo "📊 Nenhum banco encontrado. Criando novo banco..."
              npm run init:db --workspace=apps/api || echo "⚠️ Erro ao criar banco"
              echo "⚠️ ATENÇÃO: Banco novo criado! Se você tinha dados, restaure do backup."
            else
              echo "✅ Banco de dados preservado. Não foi necessário criar novo banco."
            fi
            
            # Executar migrações pendentes
            echo "🔄 Executando migrações..."
            npm run migrate:proposal-observacoes --workspace=apps/api || echo "⚠️ Migração pode já ter sido executada"
            
            # Reiniciar aplicações PM2
            echo "🔄 Reiniciando aplicações..."
            pm2 restart all
            
            # Salvar configuração PM2
            pm2 save
            
            # Verificar status
            echo "📊 Status das aplicações:"
            pm2 status
            
            echo "✅ Deploy concluído!"

      - name: Verify deployment
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT || 22 }}
          script: |
            echo "🔍 Verificando logs das aplicações..."
            pm2 logs --lines 20 --nostream

